import React from 'react';
import { Box, Typography } from '@mui/material';

export const VisitorMaster: React.FC = () => {
    return (
        <Box sx={{ p: 3 }}>
            <Typography variant="h4">Visitor Master</Typography>
            <Typography>Coming soon...</Typography>
        </Box>
    );
};
